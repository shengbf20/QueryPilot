# QueryPilot 阶段三：自动化评测与人机复核平台

> 记录范围：金标问答加载、Execution Match (EX) 评测引擎、Eval-Agent 归因、分级人机复核与案例回流。  
> 记录时间：2026-08-08 规划落档；Step 1–5 完成  
> 状态：🚧 进行中（步骤 1–5 完成；步骤 6 基线待收口）  
> 前置依赖：阶段二已完成（`ask()` / L1·L2 围栏 / CLI 可端到端跑通）

---

## 一、阶段目标

建立可复跑的工业级评测闭环，回答阶段二尚未覆盖的两个问题：**对照金标算不算对**、**错在哪里、如何回流改进**；并顺带采集端到端耗时基线，为体验分与阶段四优化提供数据（本阶段不做缓存/并行优化本身）。

主链路：

**金标问答对 → 批量跑 Agent SQL + 金标 SQL → Execution Match →（不一致时）Eval-Agent 归因 → 分级分流 / 人工复核 → 优质案例回流 Few-Shot**

| 交付物 | 对应 README | 验收要点 |
|--------|-------------|----------|
| 金标加载与用例集 | Step 9 前置 | 解析 `data/Q&A.xlsx`（或约定等价格式）；统一 `question` / `gold_sql` 字段 |
| EX 评测引擎 | Step 9 | Agent 结果集 vs 金标结果集比对；输出 EX 准确率、明细、失败清单 |
| 延迟基线采集 | Step 9 附带 | 评测报告含每题 / 分位数端到端耗时（prune / LLM / 围栏 / execute 可选拆分） |
| Eval-Agent 归因 | Step 10 | EX 不一致时输出错误类型 + Markdown 诊断报告 |
| 人机复核分流 | Step 11 | 高分自动过 / 模糊人工核 / 低分 Bad Case；修正案例可回流 |
| 评测入口 | — | CLI / 脚本可一键跑子集或全量；pytest 覆盖比对与加载逻辑 |

**本阶段不做**：Chat UI（阶段五）、Redis 缓存与复杂查询并行（阶段四）、最终盲测打包与 >90% 正式验收（阶段五 Step 15，本阶段先出基线数字）。

**须遵守约定**：跨表默认只按 `pty_id`/`org_id` Join（不加默认 `data_dt`）；评测执行走只读 DuckDB；不改 `data/` 原始 CSV；金标文件只读加载；Agent 侧仍只依赖 `load_metadata()` / 既有 `ask()`。

**与阶段二缺口的对应**：

| 阶段二缺口 | 本阶段落点 |
|------------|------------|
| 无标准答案，准确率未验 | Step 9 EX Match |
| 错误不可解释、难迭代 | Step 10 归因 + Step 11 回流 |
| 时间指标未系统采集 | Step 9 报告附带延迟；压测优化留给阶段四 |

---

## 二、文件框架（计划落地）

```
querypilot/
├── eval/
│   ├── __init__.py           # 对外导出 run_eval / compare_results 等
│   ├── models.py             # EvalCase / MatchResult / EvalReport / Diagnosis
│   ├── dataset.py            # 加载 Q&A.xlsx → 标准化用例列表
│   ├── execution_match.py    # 结果集比对（Key 对齐、集合/数值容差）→ EX
│   ├── runner.py             # 批量：ask() + 执行金标 SQL + 计时 + 汇总报告
│   ├── eval_agent.py         # EX 失败时归因分析（LLM）→ Markdown
│   └── review.py             # 分级分流 + Bad Case / 回流接口（可先文件级）
│
├── agent/pipeline.py         # 复用 ask()；评测侧尽量不改 Pipeline 语义
├── db/connection.py          # 复用 execute（只读）
└── cli.py                    # 增补 eval 子命令（或独立 scripts 入口）

metadata/few_shots/           # Step 11 回流写入目标（人工确认后）
data/Q&A.xlsx                 # 赛题金标问答对（只读；若路径变更在 dataset 中配置）

tests/
├── test_eval_dataset.py
├── test_execution_match.py
├── test_eval_runner.py       # 可用假 client / 小子集
└── test_eval_agent.py

scripts/
├── demo_eval.py              # 子集冒烟评测
└── （可选）export_eval_report.py
```

配套产物（运行时生成，建议 gitignore 报告目录或仅提交样例）：`logs/eval_reports/` 或 `artifacts/eval/` 下的 JSON/Markdown 报告。

**刻意不做（可选/后续）**：完整 Web 复核 UI（阶段五可并入）；向量检索 Few-Shot（阶段二遗留，回流可先追加 YAML）；阶段四缓存与并行。

---

## 三、预计完成步骤（原计划）

1. **金标与比对底座**：`dataset.py` 解析问答对；`execution_match.py` 定 EX 规则（列/行对齐、数值容差、空结果约定）；单测用构造用例覆盖「同结果 / 异结果」。
2. **EX 评测引擎 + 延迟采集**：`runner.py` 批量调用 `ask()` 与金标 SQL 执行；汇总 EX%、分难度（若金标有标签）、失败清单；报告写入每题耗时与 p50/p95。
3. **评测 CLI / Demo + 测试**：`querypilot eval` 或 `scripts/demo_eval.py`；子集可复跑；加载与比对 pytest 通过。
4. **Eval-Agent 归因**：EX≠1（或 Agent 失败）时调用子 Agent，输出错误类型（时间窗 / Join / 聚合口径 / 过滤条件等）与 Markdown 诊断。
5. **分级复核与案例回流**：按 EX / 置信度分流（高分自动过、模糊待核、低分 Bad Case）；人工修正 SQL/问句写入回流队列，确认后进入 `few_shots`。
6. **基线收口**：约定子集或全量跑分；落档基线准确率与延迟；更新本日志进度与距 >90% 的差距清单。

**建议顺序**：1 → 2 → 3 先拿到客观 EX（与延迟基线），再 4 → 5，最后 6；避免无数字时先堆归因与复核界面。

---

## 四、进度记录

| 步骤 | 状态 | 一句话 |
|------|------|--------|
| 1 金标与比对底座 | ✅ | Q&A.xlsx → EvalCase；EX 比对；单测加固（34）+ 别名/bool P0 修复 |
| 2 EX 引擎 + 延迟采集 | ✅ | run_case/run_eval + 报告落盘 + live/demo 冒烟 |
| 3 CLI / Demo + 测试 | ✅ | `querypilot eval` + demo；P1：默认保存/旗标互斥/live eval 冒烟 |
| 4 Eval-Agent 归因 | ✅ | 启发式+可选 LLM；CLI `--diagnose`；`test_eval_agent` |
| 5 分级复核与回流 | ✅ | 三桶分流 + 队列落盘 + Few-Shot 回流；CLI review |
| 6 基线收口 | ⏳ | 待开始 |

---

### 步骤 1：金标与比对底座

**解决什么问题 / 交付成果**

- 问题：阶段二只有冒烟问句，无法对照赛题金标计算客观准确率；结果集比对规则未工程化。
- 交付：`load_qa_cases()` / `cases_from_records()`；`compare_results()` / `execution_match()`；`EvalCase` / `MatchResult`。

**涉及文件**

| 文件 | 新旧 | 说明 |
|------|------|------|
| `querypilot/eval/models.py` | 新 | 评测数据模型：`EvalCase`（金标用例）、`MatchResult`（EX 比对结果） |
| `querypilot/eval/dataset.py` | 新 | 金标加载：读 `Q&A.xlsx` / 记录列表 → `EvalCase`；xlsx 与 records **共用别名表** |
| `querypilot/eval/execution_match.py` | 新 | EX：列对齐、行多重集合、数值容差、soft score；**bool 不与 1/0 互比** |
| `querypilot/eval/__init__.py` | 已有 | 由空壳改为导出 `load_qa_cases` / `compare_results` 等公开 API |
| `tests/test_eval_dataset.py` | 新 | 金标加载（构造表 + 实文件 7 条；扩展别名 / `extras` / 空表 / 双入口一致） |
| `tests/test_execution_match.py` | 新 | EX 比对（同/异、列重排、容差、空集；单侧空集、有序行数、列名 casefold、Decimal/千分位、bool≠int、kwargs） |
| `pyproject.toml` / `requirements.txt` | 已有 | 增加依赖 `openpyxl`（读 xlsx） |

**验收**

```powershell
pytest tests/test_eval_dataset.py tests/test_execution_match.py -q
# 预期：34 passed
```

---

### 步骤 2：EX 评测引擎 + 延迟采集（规划与脚手架）

**目标**

批量跑金标：`ask(question)` + `execute(gold_sql)` → EX Match → 汇总 **EX%** 与 **耗时分位数**（为阶段四/五留基线）。本步不做 CLI/Demo（Step 3）、不做 Eval-Agent（Step 4）。

**完成路线（填空时按序）**

| 子步 | 内容 | 验证 |
|------|------|------|
| 2.1 | 实现 `run_case`：计时 ask → 执行金标 SQL → `compare_results` → `CaseEvalResult` | 假 `ask_fn`/`execute_fn` 单测：匹配/不匹配/ask 失败/金标 SQL 失败 |
| 2.2 | 实现 `run_eval`：加载/`limit` 切片 → 循环 `run_case` → `summarize` | 小用例列表得到 `EvalReport.accuracy` / `failed_ids` / `p50_ms` |
| 2.3 | （可选）`EvalReport` 落盘 JSON 到 `logs/eval_reports/` | 写文件可读回 |
| 2.4 | 实库冒烟（可 skip）：`limit=1~2` + live LLM | 报告字段齐全，不要求准确率达标 |

**约定**

- 复用既有 `ask()` / `execute()`；经 `ask_fn` / `execute_fn` 注入，便于单测不打真实 LLM。
- 金标与 Agent 共用只读 DuckDB；`max_rows` 两端一致，避免截断造成假阴性。
- Agent 失败或金标执行失败 → 该题 `matched=False`，写入 `error`/`stage`，不中断整批。
- 延迟：至少 `total_ms`；拆分 `ask_ms` / `gold_execute_ms` / `match_ms`（脚手架模型已预留）。
- **不做**：缓存、并行、归因 LLM、人机复核 UI。

**涉及文件**

| 文件 | 新旧 | 说明 |
|------|------|------|
| `querypilot/eval/runner.py` | 已有 | 2.1/2.2：`run_case`/`run_eval`；2.3：`save_eval_report`/`load_eval_report`/`save_path` |
| `querypilot/eval/models.py` | 已有 | `TimingInfo` / `CaseEvalResult` / `EvalReport`（本步沿用） |
| `querypilot/eval/__init__.py` | 已有 | 导出报告读写 API |
| `tests/test_eval_runner.py` | 已有 | 假 client 全路径 + JSON 往返；live `limit=1`（无 Key/库则 skip） |
| `scripts/demo_eval.py` | 新 | 子集冒烟：`--limit` + 默认落盘 `logs/eval_reports/` |

**验收**

```powershell
pytest tests/test_eval_runner.py -q
# 假 client 必过；live 冒烟有 Key+DB+Q&A 时跑

python scripts/demo_eval.py --limit 2
```

---

### 步骤 3：评测 CLI / Demo

**解决什么问题 / 交付成果**

- 问题：Step 2 引擎需可一键复跑入口，便于答辩与本地子集冒烟。
- 交付：`querypilot eval`；`format_eval_report`；`demo_eval` 复用同一摘要；CLI mock 单测 + live 冒烟。

**完成路线**

| 子步 | 内容 | 状态 |
|------|------|------|
| 3.1 | `eval` 参数：`--limit` / `--path` / `--max-rows` / `--output` / `--no-save` / `--max-few-shots` | ✅ |
| 3.2 | `format_eval_report`（EX% / failed / p50·p95 / 逐题 OK·FAIL） | ✅ |
| 3.3 | `main` → `run_eval` → 打印 → `save_eval_report` | ✅ |
| 3.4 | `demo_eval` 复用格式化 | ✅ |
| 3.5 | 本日志收口 + live 验收 | ✅ |

**约定**

- 复用 `run_eval` / `save_eval_report`；不新造评测引擎。
- 评测后由 CLI/`demo_eval` 调用 `save_eval_report`：`--output PATH` 指定路径，默认 `logs/eval_reports/eval_*.json`；`--no-save` 不写文件；CLI 上二者互斥。
- 退出码：评测跑完即 `0`（含 EX 失败题）；与 `demo_eval` 一致。
- **不做**：并行、归因 LLM、难度过滤 UI、Web 复核。

**涉及文件**

| 文件 | 新旧 | 说明 |
|------|------|------|
| `querypilot/cli.py` | ✏️ | `eval` 子命令 + `format_eval_report`；`--output`/`--no-save` **互斥** |
| `scripts/demo_eval.py` | ✏️ | 复用 `format_eval_report` |
| `tests/test_cli.py` | ✏️ | eval 解析/格式化/mock；P1：默认保存、互斥、live eval smoke |

**验收（已跑）**

```powershell
pytest tests/test_cli.py -q
# mock + P1；live eval/ask 有 Key+DB 时跑

python -m querypilot.cli eval --no-save
# 实测：EX 1/7，exit 0；不要求本步准确率达标

python -m querypilot.cli eval --limit 1 --output logs/eval_reports/step3_closeout.json
# 报告可读回（total/accuracy/p50_ms 等字段齐全）
```


---

### 步骤 4：Eval-Agent 归因

**目标**

对 EX 失败（或 ask/金标失败）题做错误归因，输出结构化 `Diagnosis` + Markdown，降低人工逐题读 SQL 的成本。本步**不改**取数 Agent 权重；回流改 Few-Shot 属 Step 5。

**完成路线**

| 子步 | 内容 | 状态 |
|------|------|------|
| 4.1 | 错误类型枚举 + `classify_heuristic` | ✅ |
| 4.2 | `diagnose_case`（启发式 + 可选 LLM，失败回退） | ✅ |
| 4.3 | `diagnose_failures` / `save_diagnoses` | ✅ |
| 4.4 | CLI `--diagnose` / `--no-llm-diagnose` / `--diagnose-output` | ✅ |

**错误类型**

`column_mismatch` / `row_mismatch` / `schema_hallucination` / `join_error` / `time_filter` / `aggregation` / `filter_predicate` / `agent_failed` / `gold_failed` / `unknown`

**涉及文件**

| 文件 | 新旧 | 说明 |
|------|------|------|
| `querypilot/eval/models.py` | 已有 | 新增 `Diagnosis` |
| `querypilot/eval/eval_agent.py` | 新 | 启发式分类、LLM 增强、Markdown、批量与落盘 |
| `querypilot/eval/__init__.py` | 已有 | 导出 diagnose API |
| `querypilot/cli.py` | 已有 | `--diagnose` 接线；与 `--no-save` 互斥规则扩展 |
| `tests/test_eval_agent.py` | 新 | 启发式/假 LLM/回退/批量/落盘 |
| `tests/test_cli.py` | 已有 | diagnose 解析与 mock main |

**验收**

```powershell
pytest tests/test_eval_agent.py tests/test_cli.py -q -k "not live"
# 预期：非 live 全绿

python -m querypilot.cli eval --limit 2 --diagnose --no-llm-diagnose --no-save
# 打印 EX 摘要 + 失败题 Markdown 归因（不调 LLM、不落盘）
```

---

### 步骤 5：分级复核与案例回流

**目标**

按 EX / 归因置信度分流（文件级 HITL，无 Web UI）：高分自动过、模糊待核、低分 Bad Case；人工确认后将优质 SQL 追加到 `metadata/few_shots/examples.yaml`。

**分流规则**

| 桶 | 条件 | 动作 |
|----|------|------|
| `auto_pass` | `matched=True` | 标记通过；默认不自动写 Few-Shot |
| `needs_review` | 未匹配且置信度 ≥ 0.60 | 待人工 `approve` / `reflux` |
| `bad_case` | 未匹配且置信度 < 0.60（或无诊断） | Bad Case 队列，默认不回流 |

**完成路线**

| 子步 | 状态 |
|------|------|
| 5.1 `route_case` / `build_review_queue` | ✅ |
| 5.2 队列落盘 `logs/review/` | ✅ |
| 5.3 `append_few_shot` / `approve_and_reflux`（question 去重） | ✅ |
| 5.4 CLI `eval --review`；`review build|reflux|approve` | ✅ |

**涉及文件**

| 文件 | 新旧 | 说明 |
|------|------|------|
| `querypilot/eval/models.py` | 已有 | 新增 `ReviewTicket` / `ReviewQueue` 与桶常量 |
| `querypilot/eval/review.py` | 新 | 分流、队列读写、Few-Shot 追加/批准回流 |
| `querypilot/eval/__init__.py` | 已有 | 导出 review API |
| `querypilot/cli.py` | 已有 | `--review`；子命令 `review build/reflux/approve` |
| `tests/test_eval_review.py` | 新 | 分流边界、队列、去重回流、approve/reject |
| `tests/test_cli.py` | 已有 | `--review` 与 `review reflux` mock/集成 |
| `.gitignore` | 已有 | 忽略 `logs/review/` |

**验收**

```powershell
pytest tests/test_eval_review.py tests/test_cli.py -q -k "not live"

# 评测并生成复核队列（启发式归因，不落盘）
python -m querypilot.cli eval --limit 2 --review --no-llm-diagnose --no-save

# 人工确认后回流（示例；会改 few_shots，慎用正式库）
# querypilot review approve --queue logs/review/queue_xxx.json --case-id 2
# querypilot review reflux --question "..." --sql "SELECT ..."
```

---


## 五、验收口令（阶段结束时填写）

```powershell
# Step 1–2
pytest tests/test_eval_dataset.py tests/test_execution_match.py tests/test_eval_runner.py -q
python scripts/demo_eval.py --limit 2

# Step 3 CLI
pytest tests/test_cli.py -q
querypilot eval --limit 2
# 或: python -m querypilot.cli eval --limit 2

# Step 4
pytest tests/test_eval_agent.py -q
# querypilot eval --limit 2 --diagnose --no-save

# Step 5
pytest tests/test_eval_review.py -q
# querypilot eval --limit 2 --diagnose --no-llm-diagnose --review --no-save
```





**基线指标（跑完后填）**

| 指标 | 目标（赛题/规划） | 本阶段实测 |
|------|-------------------|------------|
| EX 准确率 | 最终 >90%（阶段五收口） | _待测_ |
| 简单查询耗时 | 分钟级内（体验向秒级优化属阶段四） | _待测 p50/p95_ |
| 中等 / 复杂查询耗时 | 同上 | _待测_ |
| 人工复核占比 | 规划降低 80%+（相对纯人工） | _待估计_ |
