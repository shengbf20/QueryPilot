# 11：口径对齐与双路径收口

> 接 `logs/09`（Extra 盈亏金标）、`logs/10`（双路径 v1）。  
> 原则不变：不改 `ask()` 编排；Agent 只复制规则、复用函数。

---

## 1. 期间盈亏统一为括号式

正确口径：

```text
aset_pft = end_nm + end_fc - (bgn_nm + bgn_fc) + aset_out - aset_in
```

无括号时（`- bgn_nm + bgn_fc`）会把期初外币加成加项，有 `fc_pur_aset` 时多算 `2 × bgn_fc`。不再保留「对齐旧金标 quirk」。

同步处：官方题 3、Extra H01 / Extra2 FH01（09 已改）、Few-Shot、`metrics.yaml`、快路径规则 17、Agent 规则 17、`pnl_fix`（直觉式与旧 quirk 都改写成等价减项）。

---

## 2. Agent 相对 v1 的收口

仍在 `querypilot/agentic/`，不折进 `ask()`。

- 首轮带剪枝 Schema + Few-Shot；**续聊换题重新剪枝**，不以首轮表白名单当权限。
- L1 对 Agent 用整库目录；`search_schema` 并集，不冲掉已有表。
- `run_sql` 成功后对照题面再 `finish`（执行成功 ≠ 答对）。
- 分桶只输出有数据的组，不用 UNION 补空档（对齐官方题 2）。
- 对话页思维链默认折叠。

---

## 3. 全量评测（对齐后）

官方金标保留精确 Few-Shot；Extra / Extra2 关短路、`fs=3`；Extra3 拒绝+警告算对。`workers=4`。

| 集 | fast | agent |
|---|---|---|
| 官方 7 | 7/7 | 7/7 |
| Extra36 | 36/36 | 36/36 |
| Extra2 | 40/40 | 40/40 |
| Extra3 | 24/24 | 24/24 |

合计 **214/214**。报告：`logs/eval_reports/{gold,extra36,extra2,extra3}_{fast,agent}.json`。
