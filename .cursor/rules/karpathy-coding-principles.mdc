---
description: Karpathy-style coding principles for QueryPilot agents (think / simplify / surgical / goal-driven)
alwaysApply: true
---

# Agent 编码原则（强制参考）

完成本项目时，严格遵循仓库根目录 **`AGENTS.md`**。摘要如下；细节与自检标准以该文件为准。

## 1. 编码前思考

不要假设；不要隐藏困惑；呈现权衡。不确定就问；有歧义就列出解释；有更简方案就提出；困惑就停下澄清。

## 2. 简洁优先

最少代码解决问题。不超额功能、不为一用抽象、不加未要的可配置性、不为不可能场景堆错误处理。能 50 行勿写 200 行。资深工程师若嫌复杂 → 简化。

## 3. 精准修改

只改请求所需。不顺手「改进」邻接代码/格式；不重构没坏的；匹配现有风格。只删自己改动造成的孤儿导入/变量；预存死代码只提不删（除非被要求）。每行改动须能追溯到用户请求。

## 4. 目标驱动执行

先定可验证成功标准再动手。例：「修 bug」→「先写复现测试再让它通过」。多步任务用 `步骤 → 验证` 列出；能跑 pytest / demo 的写完即验。
