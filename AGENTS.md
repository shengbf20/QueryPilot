# QueryPilot Agent 编码原则

> 供完成本项目的 Agent / AI 遵循。  
> 源自 [andrej-karpathy-skills](https://github.com/forrestchang/andrej-karpathy-skills)（基于 Andrej Karpathy 对 LLM 编码常见失败模式的观察），并结合本仓库约定微调。

**权衡：** 这些原则偏「谨慎正确」，而非「尽快堆代码」。对明显琐碎的改动可自行判断，不必过度仪式化。

---

## 1. 编码前思考

**不要假设。不要隐藏困惑。呈现权衡。**

实现之前：

- **明确说明假设** — 不确定就询问，不要猜测后直接开写
- **呈现多种解释** — 存在歧义时列出选项，不要默默选一个
- **适时提出异议** — 若有更简单做法，说出来；该反对时反对
- **困惑时停下来** — 指出不清楚之处并要求澄清

自检：若你在「猜用户想要什么」，先停下来问。

---

## 2. 简洁优先

**用最少的代码解决问题。不要过度推测。**

对抗过度工程：

- 不要添加要求之外的功能
- 不要为一次性代码创建抽象
- 不要添加未要求的「灵活性」或「可配置性」
- 不要为不可能发生的场景做错误处理
- 若 200 行可以写成 50 行，重写它

**检验标准：** 资深工程师会觉得过于复杂吗？如果是，简化。

何时加复杂度：只在真正需要时。需求以后出现再重构，不要提前设计。

---

## 3. 精准修改

**只碰必须碰的。只清理自己造成的混乱。**

编辑现有代码时：

- 不要「顺便改进」相邻代码、注释或格式
- 不要重构没坏的东西
- 匹配现有风格，即使你更偏好另一种写法
- 若发现无关死代码，提一下 —— 不要擅自删除

当你的改动产生孤儿代码时：

- 删除**因你的改动**而变得无用的导入 / 变量 / 函数
- 不要删除预先存在的死代码，除非被要求

**检验标准：** 每一行修改都应能直接追溯到用户的请求。

---

## 4. 目标驱动执行

**定义成功标准。循环验证直到达成。**

将指令式任务转化为可验证目标：

| 不要这样做… | 转化为… |
|-------------|---------|
| 「添加验证」 | 「为无效输入编写测试，然后让它们通过」 |
| 「修复 bug」 | 「编写能复现 bug 的测试，然后让它通过」 |
| 「重构 X」 | 「确保重构前后测试都能通过」 |

多步骤任务先写短计划：

```
1. [步骤] → 验证: [检查]
2. [步骤] → 验证: [检查]
3. [步骤] → 验证: [检查]
```

强成功标准让 Agent 能独立循环；弱标准（「让它工作」）只会不断回来问。

结合本仓库：阶段交付以 `logs/02-…md` 等记录中的验收要点为准；能 pytest / demo 验证的，写完就跑。

---

## 与本仓库的衔接（简要）

- 阶段规划与进度：`README.md`、`logs/`
- 元数据只经 `load_metadata()`；跨表默认按 `pty_id` / `org_id` Join，勿默认加 `data_dt`
- 阶段二主目录：`querypilot/agent/`、`querypilot/safety/`（剪枝在 `metadata_engine/`）
- 不要提交密钥；不要改 `data/` 原始 CSV

---

**原则是否生效的信号：** diff 更干净、因过度设计导致的返工变少、澄清问题出现在实现之前而非翻车之后。
