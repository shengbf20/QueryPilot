# QueryPilot metadata root
# Subdirectories:
#   tables/   — per-table YAML definitions (aliases, columns, descriptions)
#   metrics/  — metric tree (atomic & derived metrics)
#   few_shots/ — CoT few-shot examples for NL2SQL
